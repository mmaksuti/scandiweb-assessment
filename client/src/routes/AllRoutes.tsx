import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';

const AllRoutes = () => {
    return (
        <Router>
            <Routes>
            </Routes>
        </Router>
    );
}

export default AllRoutes;